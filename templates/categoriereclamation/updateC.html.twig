{% extends 'base.html.twig' %}

{% block title %}Modifier  Categorie de Reclamation{% endblock %}

{% block body %}

<div style="display: flex;align-items: center;justify-content: center;margin-top :150px">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Modifier   Categorie de Reclamation :</h5>

              <!-- General Form Elements -->

                

              {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">libelle</label>
                  <div class="col-sm-10">
              {{ form_widget(f.libelle,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>

                 

                  <div class="col-sm-10">
              {{ form_widget(f.modifier,{'attr': {'class':'btn btn-outline-success'}}) }}
                  </div>
                </div> 

                {{form_end(f) }}
                </form><!-- End General Form Elements -->

            </div>
          </div>

        </div>

{% endblock %}