{% extends 'base.html.twig' %}

{% block title %}Modifier reclamation{% endblock %}

{% block body %}

<div style="display: flex;align-items: center;justify-content: center;margin-top :150px">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Modifier reclamation :</h5>

              <!-- General Form Elements -->

                

              {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">email</label>
                  <div class="col-sm-10">
              {{ form_widget(f.emailReclamation,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>

                 {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">objetR</label>
                  <div class="col-sm-10">
              {{ form_widget(f.ObjetReclamation,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>

                   {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">contenue</label>
                  <div class="col-sm-10">
              {{ form_widget(f.ContenueReclamation,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>



                  <div class="col-sm-10">
              {{ form_widget(f.modifier,{'attr': {'class':'btn btn-outline-success'}}) }}
                  </div>
                </div> 

                {{form_end(f) }}



          

              </form><!-- End General Form Elements -->

            </div>
          </div>

        </div>

{% endblock %}